<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lego Collection</title>
  <link rel="stylesheet" href="/css/main.css" />

</head>

<body tabindex="-1">

  <%- include('partials/navbar',{page: ''}) %>

  <div class="container h-[80vh] mx-auto p-4 grid place-items-center">
      <div class="hero bg-base-200 rounded-lg p-6">
        <div class="hero-content flex flex-col py-12">
          <h1 class="text-5xl font-bold"><%= locals.session.user.userName %></h1>
          <p>User History: <%= locals.session.user.email %></p>
        </div>
      </div>
      <table class="table-fixed w-full border-collapse rounded-lg p-6">
        <thead class="bg-base-200">
          <th class="p-4 border-2">Login Date/Time</th>
          <th class="p-4 border-2">Client Information</th>
        </thead>
        <% for(const row of locals.session.user.loginHistory) { %>
        <tr class="">
          <td class="p-4 border-2"><%= row.dateTime %></th>
          <td class="p-4 border-2"><%= row.userAgent %></th>
        </tr>
        <% } %>
      </table>
    </div>
</body>

</html>
